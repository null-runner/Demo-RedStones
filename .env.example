# Database (Neon)
# Pooled URL — usato dall'app runtime (connection pooling)
DATABASE_URL=postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require

# Direct/Unpooled URL — SOLO per drizzle-kit migrations (DDL non supportato dal pooler)
DATABASE_URL_UNPOOLED=postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require

# Auth.js
# Generare con: openssl rand -base64 32
AUTH_SECRET=

# URL dell'applicazione (usato da Auth.js per callback)
AUTH_URL=http://localhost:3000

# Gemini API (Google AI Studio — https://aistudio.google.com/)
GEMINI_API_KEY=

# App URL (esposta al client via NEXT_PUBLIC_)
NEXT_PUBLIC_APP_URL=http://localhost:3000
